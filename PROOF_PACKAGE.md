# Persistent-event 标签覆盖结构：证明包

日期：2026-09-17。目标：对部分观测二元轨迹诱导的定长持续事件，给出精确覆盖条件、两两充分性的可检查刻画，以及这些结论的适用边界。

## Claim

本轮证明以下问题特定结论：最小见证的区间表示；任意有效单正标签覆盖可缩减至两个覆盖窗口；当 $K\ge2L-1$ 时，完整二元标签集合恰由有效一元/二元覆盖约束及确定标签描述；在这一范围内，两两充分性等价于逐标签的私人游程见证条件。参数条件对任意观测结构的统一保证是尖锐的。即便二元描述完整，覆盖约束的连续 LP 也未必给出精确风险界。

## Status

**PROVABLE AFTER WEAKENING / EXTRA ASSUMPTION**：完整性与两两充分性刻画要求 $K\ge2L-1$，且仅先刻画二元可行集；没有把完整性推广至全部参数或连续松弛。表示定理和两点覆盖约化对所有 $1\le L\le K$ 成立。

“本轮给出完整证明”与“已核实文献首创”分别判断。本文不宣称一般区间刺点、形态学、布尔闭包或最小开停机多面体的新基础理论。

## Assumptions

1. $T,K,L$ 为整数，$1\le L\le K\le T$；全部评价原点为 $S=\{0,\ldots,N-1\}$，$N=T-K+1$，使用同一个 $K,L$。
2. 观测 $o\in\{-1,0,1\}^T$，$-1$ 表示未知。$\mathcal Z(o)$ 含所有逐位尊重已观测值的二元补全。没有额外总量、动力学、跨站或单调性约束。
3. $e_s(z)=1$ 当且仅当窗口 $[s,s+K-1]$ 内至少存在一个长度为 $L$ 的全一子段。区间端点均包含。
4. 标签可行集 $\mathcal E(o)=\{e(z):z\in\mathcal Z(o)\}$。理论讨论其全部模式；模型预测和权重不参与结构定义。
5. 使用二元覆盖约束时显式规定标签属于 $\{0,1\}^N$；LP 反例另行放松为 $[0,1]^N$。

## Notation

- $b_t=1\{o_t=1\}$，即所有未知位置零的基准补全；$u_t=1\{o_t\ne0\}$ 为所有未知位置一的补全。
- $\underline e=e(b)$、$\overline e=e(u)$，模糊原点集合 $A=\{s: \underline e_s<\overline e_s\}$。固定标签在任何可行向量中保持不变。
- $M=K-L+1$；可用游程起点 $r$ 满足 $0\le r\le T-L$ 且 $[r,r+L-1]$ 没有已观测零。对原点 $j$，$R_j$ 为其中位于 $[j,j+M-1]$ 的起点。
- 最小补全 $z^{(r)}$ 把可用段 $[r,r+L-1]$ 置一，其余未知位置零，并保留所有观测。它在逐位偏序下最小，不表示风险最优。
- $\alpha_r$ 为 $r$ 前紧邻的连续已观测一向左延伸后的起点，$\beta_r$ 为 $r+L-1$ 后紧邻连续已观测一延伸后的终点。于是包含指定游程的最大一段为 $[\alpha_r,\beta_r]$。
- $J_r=S\cap[\alpha_r-K+L,\beta_r-L+1]$。$P_j$ 表示由模糊标签 $j$ 单独蕴含的全部模糊标签原点。
- $\mathcal V_2$ 为满足确定标签及所有有效两两关系的二元标签集合。

## Proof Strategy

先把每个正事件缩减为一个最小底层游程，得到区间几何；再证明长窗口中，避开指定零窗口的最小游程可以共同合并。前者给覆盖语义及两点约化，后者给构造性完整性。最后用私人见证刻画两两充分性，并分别给参数范围与 LP 完整性的反例。

## Dependency Map

1. 命题1（最小见证区间）仅用自由补全和固定游程定义。
2. 命题2（覆盖充要条件）用命题1及事件单调性。
3. 命题3（两点约化）用所有 $J_r$ 均含目标原点。
4. 引理4（安全合并）额外用 $K\ge2L-1$。
5. 定理5（二元覆盖完整性及 OR 封闭）用命题2、3和引理4。
6. 定理6（两两充分性）用定理5及精确的一元蕴含。
7. 命题7（参数尖锐性）直接构造，独立于定理5。
8. 命题8（LP 不完整）直接构造与线性不等式，不依赖任何求解器输出。

## Proof

### 命题1：最小游程的标签影响是一个区间

对任意可用 $r$，

$$e_s(z^{(r)})=\underline e_s\vee1\{s\in J_r\},\qquad s\in S.$$

**证明。** 将未知位置一只发生在指定段内。因此不同于 $b$ 的任何全一游程，必与该段相交，且属于包含该段的最大全一区间 $[\alpha_r,\beta_r]$。该区间与窗口的交集能容纳长度 $L$ 的全一段，当且仅当

$$s\le\beta_r-L+1,\qquad s+K-1\ge\alpha_r+L-1.$$

这两个不等式等价于 $s\in J_r$。其他全一段在 $b$ 中已经存在，恰由 $\underline e_s$ 处理。反向上，$s\in J_r$ 保证交集至少长 $L$，而 $\underline e_s=1$ 的原游程不会被置一操作破坏。两方向成立。$\square$

对任意 $j\in A$，$R_j$ 非空；每个 $r\in R_j$ 的 $J_r$ 都含 $j$。若任一补全令 $e_j=1$，其中必有某个 $r\in R_j$，且 $z^{(r)}\le z$。这一最小见证性质将在后文使用。

### 命题2：覆盖的精确充要条件

设 $j\in A$，$C\subseteq A\setminus\{j\}$。以下等价：

$$\forall z\in\mathcal Z(o):\quad e_j(z)\le\sum_{i\in C}e_i(z),$$

$$\forall r\in R_j:\quad J_r\cap C\ne\varnothing.$$

**证明。** 若某个 $r$ 的 $J_r$ 不与 $C$ 相交，命题1和 $\underline e_i=0$ 对 $i\in A$ 成立给出 $e_j(z^{(r)})=1$、所有 $e_i(z^{(r)})=0$，因此覆盖不成立。反向上，任意令 $e_j(z)=1$ 的补全含一个 $r\in R_j$。取 $i\in J_r\cap C$，命题1给出 $e_i(z^{(r)})=1$；由 $z^{(r)}\le z$ 及单调性，$e_i(z)=1$。$\square$

确定标签的退化情形单独处理：目标恒零时空覆盖即有效；若覆盖集合含恒一标签则一元覆盖已有效；目标恒一但覆盖只含模糊或恒零标签时，全未知置零可反驳覆盖。

### 命题3：任何有效覆盖都存在至多两点的有效子覆盖

仍令 $j\in A$、$C\subseteq A\setminus\{j\}$。若 $C$ 有效，定义它在目标两侧的最近原点

$$i^- =\max\{i\in C:i<j\},\qquad i^+=\min\{i\in C:i>j\},$$

忽略不存在的一侧。则 $\{i^-,i^+\}$ 仍是有效覆盖。因此非平凡最小覆盖基数只可能为 $1$ 或 $2$，不可能为 $3$ 或更大。

**证明。** 每个待击中的 $J_r$ 为含 $j$ 的区间。它若含 $C$ 中某个左侧点，便含该点与 $j$ 之间的最近左点 $i^-$；右侧同理。因此 $J_r$ 被 $C$ 击中当且仅当被这至多两个点击中。应用命题2即可。$\square$

这是条件于固定目标原点的区间性质，不是一般超图的两点覆盖结论，也不意味着全部高阶可行性约束都是覆盖型。

### 引理4：长窗口下，安全最小游程的合并仍安全

假设 $K\ge2L-1$。固定一个要求为零的窗口 $W=[s,s+K-1]$。设 $\mathcal R$ 是可用游程的有限集合，并且每个 $z^{(r)}$ 都满足 $e_s=0$。令

$$z^*=b\vee\bigvee_{r\in\mathcal R}1_{[r,r+L-1]}.$$

则 $e_s(z^*)=0$。当 $\mathcal R$ 为空时，额外假设 $e_s(b)=0$。

**证明。** 因为单个指定游程不能完整落入 $W$，它在 $W$ 内新置一的位置只可能构成一个左前缀或一个右后缀。取所有左前缀最大长度 $p$ 和右后缀最大长度 $q$，不存在则为零。单个见证安全意味着 $p,q\le L-1$。

由参数假设，$p+q\le2L-2<K$，所以两端区间不交且中间至少留一个位置。合并后的 $W$ 内，仅长度 $p$ 的前缀、长度 $q$ 的后缀被额外置一，中间保持 $b$。

假设合并后存在长度 $L$ 的全一段。若该段不同时触及左右置一区间，则它已出现在取得相应最大前缀/后缀的单个见证，或已出现在 $b$ 中，均与安全条件矛盾。若它同时触及左右区间，则中间的 $K-p-q$ 个位置全部为已观测一。取得最大前缀的见证于是单独拥有长度至少

$$p+(K-p-q)=K-q\ge K-(L-1)\ge L$$

的全一前段，已令 $e_s=1$，再次矛盾。若 $p$ 或 $q$ 为零，则后一情形不可能发生。故合并不产生该零窗口内的事件。$\square$

同一合并对任意一组指定零窗口逐个应用该论证，因此可以同时保持所有零约束。

### 定理5：长窗口下的完整二元描述与构造性实现

假设 $K\ge2L-1$。一个二元标签向量 $y$ 属于 $\mathcal E(o)$，当且仅当它满足全部确定标签，以及所有有效的

$$y_j\le y_i,\qquad y_j\le y_i+y_k.$$

此外 $\mathcal E(o)$ 对逐位 OR 封闭。

**证明，第1步：必要性。** 这些不等式按有效性定义在所有合法补全上成立，确定标签也不能改变。

**第2步：逐正标签寻找安全见证。** 设 $y$ 满足上述约束。令 $Z=\{i\in A:y_i=0\}$。对每个模糊正标签 $j$，若不存在最小见证避免 $Z$，则每个 $J_r$ 都被 $Z$ 击中；命题2说明 $Z$ 是有效覆盖，命题3进一步给出基数至多二的有效子覆盖。$y_j=1$ 而子覆盖上的标签全零，这违反假设。因此每个模糊正标签都有一个避免全部目标零标签的可用见证。恒零标签对任何合法见证均为零，无须额外加入 $Z$。

**第3步：同时实现。** 把这些见证的指定游程与 $b$ 合并。每个目标正标签的游程被保留；恒一标签由 $b$ 保留。引理4保证所有目标零标签仍为零。因此合并轨迹确实产生 $y$。无模糊正标签时直接使用 $b$。

**第4步：OR封闭。** 任取两个可行标签向量。确定标签在 OR 后不变；如果 OR 后某个 $y_j=1$，至少一个原向量的该位为一，其中所有有效覆盖均已满足，OR 不会使覆盖右侧从一变零。因此 OR 仍满足上述全部约束，由第3步可实现。$\square$

该定理是**二元集合的精确描述**，不是这些不等式的连续 LP 已等于凸包。后者由命题8否定。

### 定理6：两两充分性的私人游程充要条件

设 $K\ge2L-1$。对每个 $j\in A$，令

$$a_j=\max_{r\in R_j}(\alpha_r-K+L),\quad
b_j=\min_{r\in R_j}(\beta_r-L+1),\quad
P_j=A\cap[a_j,b_j].$$

则 $P_j$ 恰为 $j$ 单独蕴含的模糊标签集合。并且

$$\mathcal E(o)=\mathcal V_2
\quad\Longleftrightarrow\quad
\forall j\in A\ \exists r\in R_j:\ J_r\cap A=P_j.$$

满足右侧的 $r$ 称为 $j$ 的私人游程；其标签影响只包含不可避免的一元蕴含。

**证明，第1步：确定一元蕴含。** 用命题2取 $C=\{i\}$，得到 $j\Rightarrow i$ 当且仅当 $i$ 属于全部 $J_r$，即 $i\in P_j$。

**第2步：必要性。** 满足全部蕴含的二元模式可以取为模糊坐标上 $1_{P_j}$，因为完整蕴含关系具有传递性，$P_j$ 对它向上封闭。若两两层精确，此模式有合法轨迹实现。该轨迹令 $j$ 为正，因此含一个可用 $r\in R_j$；其最小见证模式既不能超出 $P_j$，又必须包含 $P_j$ 中全部被蕴含的坐标，故恰等于 $P_j$。

**第3步：充分性。** 任取满足全部蕴含的二元模式 $y$。对每个 $y_j=1$，选私人见证，其模糊正标签集合为 $P_j\subseteq\{i:y_i=1\}$。这些见证分别避开全部目标零标签，故引理4允许合并，并得到 $y$。因此两两集合无额外模式。$\square$

**有限几何检查。** 定义

$$p_j=\max\{i\in A:i<a_j\},\qquad
q_j=\min\{i\in A:i>b_j\},$$

一侧不存在时删除对应检验。私人游程存在当且仅当某个 $r\in R_j$ 满足

$$\alpha_r-K+L>p_j,\qquad \beta_r-L+1<q_j.$$

原因是 $J_r$ 总含 $P_j$；若它含任何额外左侧模糊标签，就必含最近的 $p_j$，右侧亦然。

如果不存在私人游程，则 $p_j,q_j$ 必须都存在：若仅一侧存在，该侧点将被所有见证包含，从而本应属于 $P_j$，矛盾。此时

$$e_j\le e_{p_j}+e_{q_j}$$

有效，而两个单独蕴含都无效。它给出两两层不完整的三标签证据。这个“不完整”的充分证据对任意 $K,L$ 仍有效；只有“所有私人见证存在便足够”的方向需要长窗口条件。

前后缀已观测一长度和可用游程可在 $O(T)$ 时间预处理。对 $m=|A|$ 个目标逐个枚举至多 $M$ 个游程，并二分查找边界，得到 $O(T+m(M+\log T))$ 时间、$O(T+m)$ 工作空间的判定。这里不含输出全部二元覆盖不等式的成本；也未宣称全部风险优化达到此复杂度。

### 命题7：参数条件对统一保证是尖锐的

对任意 $L\ge2$、$L\le K\le2L-2$，取 $T=K+2$ 且全部底层位未知。三个标签中，模式 $(1,0,0)$ 和 $(0,0,1)$ 可实现，其 OR $(1,0,1)$ 不可实现，却满足所有单正标签覆盖约束。

**证明。** 仍令 $M=K-L+1\le L-1$。仅把起点 $0$ 的长度 $L$ 段置一得到 $(1,0,0)$；仅把起点 $M+1$ 的长度 $L$ 段置一得到 $(0,0,1)$。

若要求 $(1,0,1)$，中间零标签排除起点 $1,\ldots,M$ 的全部游程。第一个正标签只能由起点 $0$ 的游程实现；最后一个只能由起点 $M+1$ 的游程实现。由于 $M+1\le L$，两段相接或重叠，迫使从 $0$ 到 $M+L$ 全部为一，尤其使中间窗口出现事件，矛盾。

任何被 $(1,0,1)$ 违反的单正覆盖，其覆盖侧只能使用唯一零标签1；但两端到标签1的一元蕴含分别被上述两个合法模式反驳。因此所有覆盖仍不能排除该模式。$\square$

这说明短窗口还可能具有“多个正游程共同迫使另一个事件”的桥接关系，不能只研究一个正标签被哪些零标签覆盖。

### 命题8：全部覆盖的 LP 仍可遗漏严格风险判向

取 $K=6,L=3,T=10,N=5$，全部底层位未知。此处 $M=4$ 且属于长窗口范围。所有有效二元覆盖恰由 $y_j\le y_i+y_k$、$i<j<k$ 表示（共10条）；不存在非平凡一元蕴含。令

$$c=(1,-1,1,-1,1),\qquad x=(1/2,1,1/2,1,1/2).$$

则 $x$ 满足全部覆盖和箱约束，$c^Tx=-1/2$，但任意真实标签向量满足 $c^Ty\ge0$，并能取到零。

**证明，第1步：可行二元结构。** 每个长度3的独立游程产生长度4的标签区间，边界可截断。三个查询原点 $i<j<k$ 的跨度不超过4；任一使 $j$ 为一的游程，其标签区间必触及 $i$ 或 $k$。而原点两侧极端游程可分别反驳任何非平凡一元蕴含。定理5给出完整二元描述。因此标签为一的形态是前缀一段与后缀一段的并，即零坐标是一个区间或空集，合计16种。

**第2步：整点不等式。** 全一向量的 $c$ 和为1。从中删除连续零区间等价于减去一段交替 $+1,-1$ 的和，该段和至多1。因此 $c^Ty\ge0$。例如 $y=(0,1,1,1,1)$ 可实现并取到零。

**第3步：LP反例与最优值。** $x$ 的任一覆盖右端至少为1，故满足全部约束；目标为 $-1/2$。令 $v=x_1+x_3$、$S=x_0+x_2+x_4$。覆盖约束给出 $v\le S+x_2$ 和 $v\le2S-2x_2$，按 $2/3,1/3$ 加权得 $v\le4S/3$；又 $v\le2$。故 $S-v\ge-v/4\ge-1/2$。上述点达到该值，所以 LP 下界恰为 $-1/2$。

**第4步：转成合法 Brier 比较。** 取

$$a=(0,3/4,0,3/4,0),\qquad b=(1/2,1/4,1/2,1/4,1/2).$$

均属于合法概率范围，且

$$5D(y)=1/4+c^Ty.$$

真实最小风险差为 $1/20>0$，故模型B严格更优；覆盖 LP 的最小差为 $-1/20$，且它也含有正差，因此不能判向。$\square$

该反例不属于新的数据实验；它直接否定“列出全部二元覆盖后普通LP一定等于完整轨迹风险界”。整数可行集已经完整，缺失的是凸包描述。更长的交替不等式与经典最小开停机多面体理论有关，不能据此宣称新通用切平面算法。

## Corrections or Missing Assumptions

- 覆盖基数至多2不等于全部逻辑关系都至多涉及3个标签；短窗口的桥接反例需要另外处理。
- 长窗口条件是对所有观测布局的统一保证；某些短窗口个例仍可能两两充分。
- 精确二元可行集不等于精确连续凸包，命题8禁止这一推广。
- 两两结构充分对所有线性风险权重给精确端点；结构不充分只保证存在某个可分离权重，不保证当前预测器改变排名。
- 长窗口OR封闭是**输出可行模式集合**的性质，不是声称 $e(z\vee z')=e(z)\vee e(z')$；证明通过最小见证合并，不能直接合并原始轨迹中的无关孤立一。

## Open Risks

上述数学证明为作者侧推导，已有穷举、构造族和既有结构证书交叉检查；未完成外部独立数学审稿或形式化定理证明。文献首次性仍需针对“部分观测约束下的输出像、参数分界和私人见证判据”核查；一般形态学表示、区间覆盖约化以及交替凸包不等式均有成熟背景，不能整体包装为首创。
