# 长历史收益来自哪里：O/D、近期历史与重复表示

日期：2026-09-13。本轮用8个固定普通ridge模型拆分上一轮的长历史线索，不再训练单模弛豫，不搜索正则或alpha。[配置](../../../configs/research/LONG_HISTORY_SOURCE_ABLATION_V1.json)及[执行入口](../../../scripts/research/run_long_history_source_ablation.py)在正式运行前冻结；运行状态由实际回执记录。

## 唯一主问题

给定168小时占用历史和最近一个可用D，更早的D是否仍有可利用增量？若有，能否超出重复已有O造成的表示/正则变化？本轮同时固定24条截断对照，最多定位“更近期/更早”的算法表现，不逐lag搜索或断言周周期机制。

设O_k=y[o−k:o]，D_k=d[o−k−1:o−1]。D的条数和O相同，不代表时间区间相同；D最晚为origin−2。共同上下文为日/周相位及log(1+C)，没有其他字段、区域ID或图输入。

| 模型 | 输入 | 列数（不含截距） |
|---|---|---:|
| S | O2、D1、共同上下文 | 8 |
| O_LONG | O168、D1、共同上下文 | 174 |
| D_LONG | O2、D168、共同上下文 | 175 |
| OD_LONG | O168、D168、共同上下文 | 341 |
| O_LONG_D24 | O168、D24、共同上下文 | 197 |
| O24_D_LONG | O24、D168、共同上下文 | 197 |
| O_LONG_D0 | O168、共同上下文 | 173 |
| O_LONG_DUP | O168、D1、复制已标准化的前167个O、共同上下文 | 341（174种列身份） |

## 为什么需要重复控制

两个相同特征的系数之和为θ时，ridge惩罚满足：

$$\min_{\beta_1+\beta_2=\theta}\lambda(\beta_1^2+\beta_2^2)=\frac\lambda2\theta^2.$$

因此，没有新增信息也可能因重复表示而改善预测。本轮复制标准化后的O，不重缩放；这是一个明确的替代解释控制，不声称消除了全部容量与正则混杂。有效自由度ν=tr[(G+Λ)⁻¹G]只描述训练线性估计器，既不是独立样本量，也不使各组自动等容量。

理想条件均值的平方风险差可以写成嵌套条件均值差的平方期望，因而非负；但有限样本正则线性估计器不满足这种样本外保证。负增量不能证明条件独立，模型依赖不等于整个条件分布的信息价值。相关概念参见[Fisher、Rudin与Dominici的模型类变量重要性研究](https://jmlr.org/papers/v20/18-760.html)；本轮没有复现其估计器或置信区间方法。

## 固定学习和评价协议

FIT原点169—1044逐小时，共876×275=240900训练行，12输出标签全部<1056。SELECT原点1056—1212每12小时；DEV原点1392—1548每12小时。八模型在读取SELECT前全部拟合并冻结；**SELECT仅记录表现，不选择参数、模型或输出版本**。两窗口分别报告，都已曝光，不能称独立确认。

共同341列母表示只在FIT估计均值/总体标准差和Gram/目标交叉积，各模型抽取对应列。非截距标准差≤1e−10 max(1,|均值|)的列固定置零。重复列在标准化之后复制，O_LONG_D0不使用D列或D统计进行预测。

模型预测为最后占用p加线性残差。目标按1/N缩放、对12输出求和；固定λ=0.01、截距不罚、float64、CPU两线程。共享计算后仍分别进行八个模型求解，不能记成一次拟合。S和OD_LONG是上一轮短/长ridge的身份锚点重建，不作为新的独立发现。

主比较为OD_LONG对O_LONG，**raw、四H末点宏RMSE**；raw/clip、末点/path及MAE完整并报。每窗各H先评分，再对H3/H6/H9/H12等权；禁止混合两窗平均、切换主输出或按名次删模型。

四角分解在端点MSE的均值上进行，记S00=S，S10=O_LONG，S01=D_LONG，S11=OD_LONG的对应平方风险。报告四条边际增益及J=S10+S01−S00−S11，检查两条加入顺序均还原S00−S11。不任选顺序分配“D贡献百分比”，也不把J称为因果交互。

两条24条截断路径、单D作用和重复控制均预先固定。对12个固定模型配对、每窗/H/输出/范围，报告绝对误差及平方误差的改善/增损数量、贡献和逐原点均值；检查g2=2(Y−q_b)(q_c−q_b)−(q_c−q_b)²。这里不基于标签筛样本、造门控或再训练。

## 边界与工件

阶段读取上限继承上一轮：FIT O1056/D1043；SELECT O1224/D1211；PREDICT O1548/D1547；SCORE O1560。只用O/D及静态容量三列。模型固定后可以滚动使用已经发生的评价期历史；16组DEV raw/clip预测在最后12行目标扩展前冻结。不读取旧私有权重或预测；旧公开成绩仅用于S/OD_LONG身份核验，不重新调用native。

256条正式评分、384个损失配对单元和5376条原点配对汇总全部保留。技术一致性检查不参与科学收益判断；没有1%或MAE非劣门。所有原始输入、权重和预测留本地，公开仅含配置、列映射、哈希和汇总。

尾部[1560,1747)、第三折验证/测试和Paris保持关闭。完成后返回人工审核，不启动定时任务，不使用额度重置卡，不自动下一轮。
